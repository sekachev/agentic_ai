# Модуль 5: OpenClaw - организация работы, хранения и обработки информации

---

## Структура модуля

* Рабочая папка openclaw, синхронизация и бэкап, совместная работа, клонирование агентов
	* Координация 
	* https://meet.google.com/usf-nztb-mca

* Базы данных
	* Почему xls таблицы — это тупик, а база данных - это круто?
	* Решение - реляционная база данных
	* Таблицы - сущности
	* Поля, первичный ключ, внешний ключ
	* Записи
	* Схема данных
	* Язык SQL
	* Файловая-Клиент сервер

---

| Тип         | Для чего               | Пример                          |
| :---------- | :--------------------- | :------------------------------ |
| **INTEGER** | Целые числа, ID        | `1`, `42`, `1000`               |
| **FLOAT**   | Дробные числа          | `99.99`, `0.5`                  |
| **TEXT**    | Строки                 | `Иван`, `email@test.com`        |
| **DATE**    | Даты (ISO: YYYY-MM-DD) | `2025-01-28`                    |
| **BOOLEAN** | Да/Нет                 | `true`, `false`                 |
| **JSON**    | Списки, объекты        | `[1, 2, 3]`, `{"key": "value"}` |

---
```sql
CREATE TABLE students (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    full_name TEXT NOT NULL,
    email TEXT UNIQUE NOT NULL,
    phone TEXT,
    group_id INTEGER,
    status TEXT DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (group_id) REFERENCES groups(id)
);
```

---
```sql
SELECT 
    YEAR(payment_date) AS year,
    QUARTER(payment_date) AS quarter,
    SUM(amount) AS total_revenue
FROM payments
GROUP BY year, quarter;
```