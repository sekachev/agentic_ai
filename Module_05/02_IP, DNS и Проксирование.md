# 🌐 Глава 2: IP, DNS и Проксирование (Nginx)

Эта глава — фундамент понимания того, **как интернет находит ваш сайт** и **как один сервер может управлять несколькими адресами**.

---

## 🏠 Часть 1: IP-адрес — это ваш "Почтовый индекс"

### Что такое IP-адрес?

**IP (Internet Protocol)** — это числовой адрес компьютера в интернете.

Аналогия: Это как адрес дома в реальном мире.
*   **Название улицы (домен):** `google.com` — это красиво и легко запомнить.
*   **Почтовый индекс (IP):** `142.250.185.46` — машинам нужны точные координаты.

### Как узнать свой IP?

На сервере выполните команду:
```bash
ip a
# или полная команда
ip addr show
```
Вы получите что-то вроде: `123.45.67.89` — это **внешний IP вашего VPS**.

> **Важно:** Этот IP уникален в интернете. Через него можно попасть на ваш сервер из любой точки мира.

---

## 🔍 Часть 2: DNS — телефонная книга интернета

### Зачем нужен DNS?

Люди плохо запоминают цифры, но хорошо помнят слова.  
**DNS (Domain Name System)** превращает человеческие имена (`api.tatiana.qix.ee`) в машинные адреса (`123.45.67.89`).

### Как это работает?

1.  Вы вводите в браузере: `https://api.tatiana.qix.ee`
2.  Браузер спрашивает DNS-сервер: *"Какой IP у этого домена?"*
3.  DNS отвечает: *"Это 123.45.67.89"*
4.  Браузер подключается к серверу по этому IP.

### DNS-записи (типы указателей)

В панели управления доменом (например, Cloudflare или регистратор) вы настраиваете **DNS-записи**:

| Тип записи | Назначение                        | Пример                                  |
|------------|-----------------------------------|-----------------------------------------|
| **A**      | Указывает на IPv4-адрес сервера   | `api.tatiana.qix.ee` → `123.45.67.89`   |
| **CNAME**  | Псевдоним (перенаправление)       | `www.site.com` → `site.com`             |
| **MX**     | Для почтовых серверов             | (нам не нужно в этом курсе)             |

> **Для наших задач:** Мы используем **A-записи**, чтобы привязать имя (`api.tatiana.qix.ee`) к IP вашего VPS.

---

## 🚦 Часть 3: Проблема — один IP, много сайтов

### Ситуация

У вас **один сервер** с IP `123.45.67.89`, но вы хотите разместить на нём:
*   `https://api.tatiana.qix.ee` — ваш API (FastAPI на порту 8000)
*   `https://tatiana.qix.ee` — ваш фронтенд (статические файлы)
*   `https://panel.tatiana.qix.ee` — панель Cockpit (порт 9090)

**Проблема:** Браузер подключается к серверу по IP, но как сервер узнает, какой именно сайт показать?

---

## 🔀 Часть 4: Nginx — умный швейцар (Reverse Proxy)

### Что такое Nginx?

**Nginx** — это программа-посредник, которая:
1.  Встречает **все входящие запросы** (на порту 80 и 443).
2.  Смотрит на **имя домена** в запросе (`api.tatiana.qix.ee` или `tatiana.qix.ee`).
3.  **Перенаправляет** (проксирует) запрос нужному приложению:
    *   Если домен `api.*` — отправляет на порт 8000 (FastAPI)
    *   Если домен без `api` — отдает файлы из папки `/var/www/html`
    *   Если домен `panel.*` — отправляет на порт 9090 (Cockpit)

### Аналогия: Швейцар в отеле

Представьте **отель** (ваш сервер):
*   Все гости (запросы) заходят через **главный вход** (Nginx).
*   **Швейцар** спрашивает: *"Вы к кому?"*
    *   *"К API"* → направляет в комнату 8000.
    *   *"На сайт"* → отдает брошюру из папки (статика).
    *   *"В админку"* → отправляет в комнату 9090.

---

## ⚙️ Часть 5: Как работает конфигурация Nginx

### Файл конфигурации

Nginx читает инструкции из файлов в папке `/etc/nginx/sites-enabled/`.

Пример конфига для **API**:
```nginx
server {
    listen 80;
    server_name api.tatiana.qix.ee;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Разбор строк:
*   **`listen 80;`** — Слушай входящие подключения на порту 80 (HTTP).
*   **`server_name api.tatiana.qix.ee;`** — Эта конфигурация срабатывает, если запрос пришел на домен `api.tatiana.qix.ee`.
*   **`proxy_pass http://127.0.0.1:8000;`** — Перешли запрос на локальный порт 8000 (где работает FastAPI).
*   **`proxy_set_header`** — Передаем дополнительную информацию (кто спрашивал, какой домен).

---

## 🛠 Часть 6: Практическая настройка (Пошагово)

### Шаг 1: Проверить статус Nginx
```bash
systemctl status nginx
```
🟢 Должно быть **active (running)**.

### Шаг 2: Создать конфиг для API
Откройте редактор:
```bash
nano /etc/nginx/sites-available/api
```

Вставьте содержимое:
```nginx
server {
    listen 80;
    server_name api.ваше_имя.qix.ee;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Сохраните: `Ctrl+O`, затем `Enter`. Выйдите: `Ctrl+X`.

### Шаг 3: Активировать конфиг
Создайте символическую ссылку (это как ярлык в Windows):
```bash
ln -s /etc/nginx/sites-available/api /etc/nginx/sites-enabled/
```

### Шаг 4: Проверить синтаксис
Перед перезагрузкой убедитесь, что нет ошибок:
```bash
nginx -t
```
✅ Должно быть: `syntax is ok` и `test is successful`.

### Шаг 5: Перезагрузить Nginx
```bash
systemctl reload nginx
```

---

## 🔐 Часть 7: HTTPS и SSL (Certbot)

По умолчанию ваш сайт работает по HTTP (незашифрованному протоколу).  
Чтобы включить **HTTPS** (замочек в браузере), используется инструмент **Certbot**.

### Автоматическая установка сертификата
```bash
certbot --nginx -d api.ваше_имя.qix.ee
```

Certbot:
1.  Получит бесплатный SSL-сертификат от **Let's Encrypt**.
2.  Автоматически изменит ваш конфиг Nginx, добавив `listen 443 ssl;`.
3.  Настроит автоматическое перенаправление с HTTP на HTTPS.

> **Важно:** Сертификаты обновляются автоматически каждые 90 дней.

---

## 📋 Итоговая схема

```
[Браузер пользователя]
        ↓
   🌐 DNS запрос: api.tatiana.qix.ee
        ↓
   📍 Получен IP: 123.45.67.89
        ↓
   🔐 HTTPS запрос на порт 443
        ↓
   🚦 Nginx (проверяет server_name)
        ↓
   ✅ proxy_pass → http://127.0.0.1:8000
        ↓
   🐍 FastAPI обрабатывает запрос
        ↓
   📤 Ответ возвращается через Nginx
        ↓
   🌐 Браузер показывает результат
```

---

## 🎯 Итог: Зачем всё это нужно?

| Технология | Роль                                           |
|------------|------------------------------------------------|
| **IP**     | Физический адрес сервера в интернете           |
| **DNS**    | Превращает имена в IP (человек → машина)       |
| **Nginx**  | Умный маршрутизатор (один вход, много выходов) |
| **SSL**    | Шифрование данных (замочек в браузере)         |

Теперь вы понимаете **всю цепочку от ввода адреса в браузере до ответа сервера**. Это база, без которой невозможно стать DevOps-грамотным разработчиком.

В следующей главе мы применим эти знания на практике — задеплоим первый frontend через Cockpit.
