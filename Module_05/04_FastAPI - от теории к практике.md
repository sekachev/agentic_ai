# 🚀 FastAPI: от теории к практике

Эта глава — полноценный путь от понимания API до запуска вашего собственного сервера, который работает 24/7.

---

## Часть 1: Что такое API? 🧐

### Аналогия с рестораном

Представьте, что вы пришли в ресторан.
*   **Вы** — это пользователь (или сайт/приложение).
*   **Кухня** — это сервер, где лежат продукты (база данных) и повара готовят блюда (выполняют код).

Вы не можете просто ворваться на кухню и начать рыться в холодильнике. Вам нужен посредник.
Этот посредник — **Официант**.

В мире программирования **Официант = API** (Application Programming Interface).

1.  Вы смотрите в **Меню** (Документация), чтобы узнать, что можно заказать.
2.  Вы делаете **Заказ** (Request) официанту: *"Принесите мне данные пользователя ID 5"*.
3.  Официант относит заказ на кухню, там что-то происходит.
4.  Официант возвращает вам **Блюдо** (Response) — обычно в формате JSON (текстовые данные).

**Наша задача:** С помощью AI создать такого "официанта", который будет быстро и безошибочно принимать заказы и отдавать данные.

---

## Часть 2: Почему FastAPI? ⚡️

В мире Python есть много инструментов для создания API. Но мы выбрали **FastAPI**, и вот почему это идеальный выбор для AI-assisted coding:

1.  **Скорость:** Он очень быстрый (как следует из названия).
2.  **Строгость:** Он заставляет четко указывать типы данных (число это или текст?). Для AI это огромный плюс — нейросетям проще писать правильный код, когда есть четкая структура.
3.  **Магия документации:** Это главная фишка, ради которой мы здесь.

---

## Часть 3: Автоматическая документация (Swagger UI) 📖

Обычно программисты ненавидят писать инструкции к своему коду. Это скучно и долго.
FastAPI делает это **за вас**.

Как только вы запустите сервер, FastAPI автоматически создаст красивый интерактивный сайт, где описаны все доступные команды вашего API.

### Как туда попасть?
Просто добавьте `/docs` к адресу вашего сайта:
👉 `https://api.ваше_имя.qix.ee/docs`

### Что вы там увидите?
Вы увидите страницу (Swagger UI), которая служит пультом управления.

*   **GET** (Зеленые кнопки) — Запросы на *получение* данных (как чтение страницы).
*   **POST** (Другие цвета) — Запросы на *отправку* данных (как отправка формы).

### 🛠 Ваша роль: Вы — Контролёр (QA)

Вам не нужно писать весь код руками. Ваш процесс работы будет выглядеть так:

1.  **Задача:** Вы говорите AI (ChatGPT/Claude): *"Напиши endpoint (ручку), который принимает цену товара и рассчитывает налог 20%"*.
2.  **Код:** AI генерирует Python-код.
3.  **Внедрение:** Вы вставляете его в файл `main.py`.
4.  **Проверка:** Вы идете в `/docs`, находите новую кнопку, нажимаете **"Try it out"** (Попробовать), вводите цифры и жмете **Execute**.

**Вам не нужно писать сложные скрипты для проверки.** Вы просто нажимаете кнопки в браузере и смотрите, правильно ли "Официант" принес заказ.

---

## Часть 4: 💻 Практика — Создаём свой первый API

### Шаг 1: Создаём файл main.py

На вашем сервере в папке `/root` создайте файл:
```bash
nano main.py
```

Вставьте следующий код:
```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
    return {
        "message": "It works!", 
        "student": "Tatiana", 
        "host": "api.tatiana.qix.ee"
    }

@app.get("/items/{item_id}")
def read_item(item_id: int):
    return {"item_id": item_id}
```

**Что здесь происходит?**
- `@app.get("/")` — это декоратор, который говорит: "Когда кто-то зайдет на главную страницу, выполни функцию ниже"
- `return {...}` — возвращаем данные в формате JSON (словарь Python автоматически превращается в JSON)
- `{item_id}` — это динамический параметр в URL (например, `/items/42`)

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`.

### Шаг 2: Устанавливаем зависимости

```bash
pip3 install fastapi uvicorn
```

**Что такое uvicorn?**  
Это программа-сервер, которая запускает ваш FastAPI код и слушает входящие запросы.

### Шаг 3: Тестовый запуск (в режиме разработки)

Запустите сервер вручную:
```bash
python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
```

**Разбор команды:**
- `main:app` — файл `main.py`, переменная `app`
- `--host 0.0.0.0` — слушать на всех сетевых интерфейсах (чтобы можно было подключиться извне)
- `--port 8000` — порт, на котором работает API

Теперь откройте браузер и проверьте:
- 👉 `http://ВАШ_IP:8000/` — должны увидеть JSON с сообщением
- 👉 `http://ВАШ_IP:8000/docs` — интерактивная документация
- 👉 `http://ВАШ_IP:8000/items/42` — должны увидеть `{"item_id": 42}`

**Проблема:** Стоит вам нажать `Ctrl+C` или закрыть терминал — сервер умрет.

---

## Часть 5: 🔄 Превращаем в системную службу (Systemd)

### Зачем нам это нужно?

Сейчас ваш скрипт работает, только пока открыто черное окно терминала. Стоит вам закрыть браузер или нажать `Ctrl+C` — и ваш API "умрет". Сайт станет недоступен.

**Наша цель:** Сделать так, чтобы программа работала **вечно**, запускалась сама после перезагрузки сервера и восстанавливалась, если вдруг упадет.

Для этого мы используем **Systemd** — главный менеджер процессов в Linux.

---

## Часть 6: 📄 Анатомия файла службы

Создайте файл конфигурации службы:
```bash
nano /etc/systemd/system/fastapi.service
```

Вставьте следующее содержимое:
```ini
[Unit]
Description=FastAPI Student Server
After=network.target

[Service]
User=root
WorkingDirectory=/root
ExecStart=/usr/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

### Разбор секций:

#### Секция [Unit] — Описание
*   **`Description`** — Имя службы, которое будет видно в логах.
*   **`After=network.target`** — Важная настройка! Она говорит: "Не запускай этот сайт, пока на сервере не появится интернет".

#### Секция [Service] — Самое мясо
*   **`User=root`** — От чьего имени запускать.
*   **`WorkingDirectory=/root`** — Папка, где лежит ваш файл `main.py`.
*   **`ExecStart=...`** — Сама команда запуска (полный путь к Python).
*   **`Restart=always`** — "Режим зомби". Если программа вылетит с ошибкой, Linux тут же поднимет её снова.
*   **`RestartSec=5`** — Пауза 5 секунд перед воскрешением.

#### Секция [Install] — Автозагрузка
*   **`WantedBy=multi-user.target`** — Служба должна стартовать в обычном многопользовательском режиме.

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`.

---

## Часть 7: ⚙️ Установка и запуск

Выполните эти команды по очереди:

### 1. Перечитываем настройки
Сообщаем Systemd, что появился новый файл.
```bash
systemctl daemon-reload
```

### 2. Включаем и запускаем
Команда `enable` ставит службу в автозагрузку (чтобы работала после перезагрузки VPS).
Команда `--now` запускает её прямо сейчас.
```bash
systemctl enable --now fastapi
```

---

## Часть 8: ✅ Проверка и отладка

### Как узнать, что всё работает?
Спросите статус у системы:
```bash
systemctl status fastapi
```
🟢 Если видите зеленый кружок и надпись **active (running)** — поздравляем! Вы стали системным администратором своего API.

🔴 Если видите **failed** или **dead** — где-то ошибка.

### Как читать логи?
Так как программа работает в фоне, она больше не пишет в терминал. Чтобы увидеть, что происходит (или почему она упала), используйте команду:
```bash
journalctl -u fastapi -f
```
*   `-u fastapi` — показать логи конкретно нашего юнита.
*   `-f` — (follow) следить в реальном времени. Нажмите `Ctrl+C`, чтобы выйти.

### Остановить службу
```bash
systemctl stop fastapi
```

### Перезапустить службу (после изменения кода)
```bash
systemctl restart fastapi
```

---

## Часть 9: 🌐 Финал — Проверяем в браузере

Теперь ваш сервер работает автономно. Вы можете закрыть вкладку терминала, перезагрузить компьютер — API все равно будет доступен.

Если вы настроили Nginx (как описано в предыдущей главе), проверьте результат:
👉 `https://api.ваше_имя.qix.ee`

Там должно быть сообщение: 
```json
{
  "message": "It works!",
  "student": "Tatiana",
  "host": "api.tatiana.qix.ee"
}
```

И не забудьте про документацию:
👉 `https://api.ваше_имя.qix.ee/docs`

---

## 🎓 Итоговая схема работы

```
[Код в main.py]
       ↓
   Uvicorn сервер (порт 8000)
       ↓
   Systemd следит за процессом
       ↓
   Nginx проксирует запросы (порт 80/443 → 8000)
       ↓
   Пользователь видит API по адресу api.example.com
```

---

## 📝 Практическое задание

1. Модифицируйте код `main.py`:
   - Поменяйте значение `"student"` на ваше имя
   - Поменяйте значение `"host"` на ваш реальный домен
   - Добавьте новый endpoint `/hello/{name}`, который будет возвращать `{"greeting": "Hello, {name}!"}`

2. Перезапустите службу:
   ```bash
   systemctl restart fastapi
   ```

3. Проверьте изменения в браузере и в `/docs`

---

## 🎯 Итог

*   **API** — это способ общения программ друг с другом (Официант между вами и кухней).
*   **FastAPI** — это инструмент, который строит API и сам рисует к нему инструкцию.
*   **Swagger (/docs)** — это ваше рабочее место, где вы проверяете, не нагаллюцинировал ли AI ерунды.
*   **Systemd** — превращает скрипт в бессмертную службу, которая работает всегда.
*   **Uvicorn** — сервер, который запускает FastAPI приложение.

Теперь вы умеете создавать и деплоить полноценные API! 🎉
