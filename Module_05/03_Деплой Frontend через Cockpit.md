
# 🎨  Фронтенд. Деплой через Cockpit

---

## 🏡 Шаг 1. Подготовка  (Локально)

### 1.1 Получаем код
В **Google AI Studio** (вкладка Build) скачайте ваш проект (Download ZIP) и распакуйте его у себя на компьютере.


---

### 1.2 Самая главная настройка (Base URL) ⚠️
Чтобы сайт заработал в папке (например, `my_project`), а не в корне домена, нужно поправить один файл.

1.  Откройте папку проекта в редакторе кода (VS Code).
2.  Найдите файл `vite.config.js`.
3.  Добавьте строчку `base: '/имя_папки/',` (обязательно со слешами!).

---

Пример, если вы хотите выложить сайт в папку **test**:
```javascript
export default defineConfig({
  // 👇 Добавляем эту строчку
  base: '/test/', 
  plugins: [react()],
  // ...остальной код...
})
```

---

### 1.3 Сборка (Build)
Превращаем код в готовый сайт. В терминале вашего редактора выполните:

```bash
npm install
npm run build
```

После этого у вас появится папка **`dist`**.
Внутри неё лежат файл `index.html` и папка `assets`. **Это и есть ваш готовый сайт.**

---

## 🚀 Шаг 2. Загрузка на сервер (Cockpit)

Теперь переносим эти файлы на ваш VPS, используя удобный файловый менеджер.

---

### 2.1 Заходим в панель
1.  Откройте `https://panel.ваше_имя.qix.ee`.
2.  Слева в меню выберите **Navigator** (Файловый менеджер).

---


### 2.2 Идем в папку для сайтов
В строке адреса сверху введите путь и нажмите Enter:
👉 `/var/www/html`


---

### 2.3 Создаем "папку" для сайта
Нам нужно создать папку, имя которой совпадает с тем, что вы писали в `vite.config.js` (например, `test`).

1.  Нажмите синюю кнопку с **плюсиком и папкой** (справа сверху).
2.  Введите имя (например: `test`).
3.  Зайдите внутрь этой новой папки.


---


### 2.4 Загружаем файлы
Сейчас папка пуста. Нам нужно перенести сюда содержимое вашей локальной папки `dist`.

1.  Нажмите синюю кнопку **Upload** (стрелочка вверх) или просто перетащите файлы мышкой.
2.  **ВАЖНО:** Загружайте не саму папку `dist`, а **то, что внутри неё** (`index.html` и папку `assets`).

Должно получиться как на картинке:
*   📁 `assets`
*   📄 `index.html`

---

## 🎉 Финал

Открывайте браузер и проверяйте результат!
Адрес будет таким:
👉 `https://ваше_имя.qix.ee/имя_папки/`

*(Например: `https://tatiana.qix.ee/test/`)*

