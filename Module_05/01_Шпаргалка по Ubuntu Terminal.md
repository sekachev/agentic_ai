# 🐧 Глава: Выживание в Терминале (Шпаргалка)

Терминал (или консоль) — это пульт управления сервером. Здесь нет мышки, и команды нужно печатать.
Сначала это пугает, но на самом деле вам нужно знать всего ~10 команд, чтобы чувствовать себя уверенно.

---

## 📍 Где я нахожусь? (Читаем строку перед курсором)

Вам не нужно постоянно спрашивать систему "где я?". Она пишет это в каждой строке перед мигающим курсором.

Пример:
`root@tatiana-tetkool:~/test#`

Давайте расшифруем:
1.  **`root`** — Это ВЫ (имя пользователя).
2.  **`@tatiana-tetkool`** — Это СЕРВЕР (имя машины).
3.  **`:~/test`** — Это ПАПКА, в которой вы сейчас стоите.
    *   Знак **`~`** (тильда) означает вашу "Домашнюю папку".
    *   Знак **`#`** в конце означает, что вы — главный администратор.

---

## 🧭 Навигация (Как ходить по папкам)

*   **`ls`** — (List) Осмотреться. Показывает список файлов и папок вокруг вас.
*   **`ls -la`** — Рентген. Показывает вообще все файлы (даже скрытые) и их размер.
*   **`cd имя_папки`** — (Change Directory) Зайти внутрь папки.
*   **`cd ..`** — Шаг назад. Выйти из текущей папки на уровень выше.
*   **`cd`** — Телепорт домой. Вернет вас в стартовую папку (`/root`).

---

## 📂 Работа с файлами (Руки)

⚠️ Внимание: здесь нет "Корзины". Если удалили — то навсегда!

*   **`mkdir папка`** — (Make Directory) Создать новую папку.
*   **`cp файл1 файл2`** — (Copy) Скопировать файл.
*   **`mv старое новое`** — (Move) Переместить файл. **Этой же командой файлы переименовывают!**
*   **`rm файл`** — (Remove) Удалить файл.
*   **`rm -r папка`** — Удалить папку со всем содержимым.

---

## 📝 Редактирование текста (Nano)

В Linux есть простой текстовый редактор прямо в окне терминала — **Nano**.

*   **Открыть файл:** `nano main.py`
*   **Сохранить:** Нажмите `Ctrl + O`, затем `Enter`.
*   **Выйти:** Нажмите `Ctrl + X`.

---

## ⚙️ Управление системой

*   **`python3 main.py`** — Запустить скрипт вручную (чтобы проверить ошибки).
*   **`systemctl status fastapi`** — Узнать, жив ли ваш фоновый сайт (зеленый — жив, красный — мертв).
*   **`systemctl restart fastapi`** — Перезагрузить сайт (если вы обновили код).
*   **`htop`** — Диспетчер задач (выход через кнопку `F10` или `q`).

---

## 🧙‍♂️ Секретные приемы (Магия)

Эти кнопки сделают вас профессионалом за 5 минут.

### 1. Кнопка `Tab` (Автодополнение) 🌟
Никогда не пишите длинные имена файлов полностью!
Напишите первые 2 буквы (например, `cd my_`) и нажмите **Tab**. Система сама допишет остальное.

### 2. Стрелка ВВЕРХ ⬆️
Возвращает предыдущие команды. Не нужно печатать одно и то же 10 раз. Просто листайте историю стрелками.

### 3. `Ctrl + R` (Поиск в прошлом) 🕵️‍♂️
Самая крутая команда.
1. Нажмите `Ctrl + R`.
2. Начните печатать слово из команды, которую вы делали вчера (например, `fastapi`).
3. Терминал сам найдет её в истории. Если нужно искать дальше — жмите `Ctrl + R` еще раз.
4. Нашли? Жмите `Enter`.

### 4. `Ctrl + C`
Аварийный тормоз. Если программа зависла, бегут непонятные буквы или вы просто хотите всё отменить — жмите это сочетание.
