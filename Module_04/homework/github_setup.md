# Настройка GitHub и первая загрузка проекта (Push) 🚀

Это руководство поможет вам настроить GitHub на вашем компьютере (Mac или PC), подключить локальную папку к репозиторию и отправить ваш первый код.

---

## 1. Установка и Авторизация (Authentication)

Самый современный и простой способ авторизации — использование **GitHub CLI (gh)**.

### Для Mac:
1. Откройте Терминал.
2. Установите GitHub CLI (если есть [Homebrew](https://brew.sh/)):
   ```bash
   brew install gh
   ```
3. Запустите авторизацию:
   ```bash
   gh auth login
   ```
4. Выберите: `GitHub.com` -> `HTTPS` -> `Yes` (Authenticate Git with your GitHub credentials?) -> `Login with a web browser`.
5. Скопируйте одноразовый код, нажмите Enter и вставьте его в открывшемся браузере.

### Для Windows (PC):
1. Откройте PowerShell или Терминал Windows.
2. Установите GitHub CLI через [winget](https://learn.microsoft.com/en-us/windows/package-manager/winget/):
   ```powershell
   winget install --id GitHub.cli
   ```
   *(Или скачайте установщик с [cli.github.com](https://cli.github.com/))*
3. Запустите авторизацию:
   ```powershell
   gh auth login
   ```
4. Следуйте тем же шагам, что и для Mac (выбор браузера и ввод кода).

---

## 2. Подключение папки к GitHub репозиторию

Если у вас уже есть папка с кодом на компьютере, и вы хотите "связать" её с новым репозиторием на GitHub:

1. **Создайте репозиторий на GitHub:**
   - Зайдите на [github.com/new](https://github.com/new).
   - Дайте имя репозиторию (например, `my-ai-agent`).
   - НЕ ставьте галочки "Initialize this repository with README, .gitignore" (мы это сделаем сами).

2. **Свяжите локальную папку с GitHub:**
   В терминале перейдите в вашу папку (`cd path/to/your/folder`) и выполните:

   ```bash
   # Инициализируем Git в папке
   git init

   # Создаем основную ветку main
   git branch -M main

   # Добавляем адрес вашего удаленного репозитория
   # (Замените URL на адрес вашего созданного репозитория)
   git remote add origin https://github.com/ВАШ_ЛОГИН/ИМЯ_РЕПОЗИТОРИЯ.git
   ```

---

## 3. Первый Push (Загрузка кода)

Теперь, когда папка "знает" о GitHub, отправим файлы:

```bash
# 1. Добавляем все файлы в индекс для отслеживания
git add .

# 2. Фиксируем изменения (Создаем "снимок" кода с описанием)
git commit -m "Initial commit: My first project files"

# 3. Отправляем код на GitHub
git push -u origin main
```

---

## 4. Памятка: Регулярный рабочий цикл

Когда вы внесли изменения в код и хотите их сохранить:
1. `git add .` — подготовить всё.
2. `git commit -m "что именно я изменил"` — прокомментировать.
3. `git push` — отправить.

> [!TIP]
> Если вы работаете в VS Code, вы можете использовать вкладку **Source Control** (Ctrl+Shift+G) для визуального выполнения этих действий.
