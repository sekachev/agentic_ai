01-08 Лекция: Технические основы LLM и API
Дата и время: 2026-01-08 09:18:32
Расположение: [Вставить Расположение]
Инструктор: [Вставить Имя]
📝 Краткое изложение
Лекция посвящена техническим основам работы современных языковых моделей (LLM) и чат-интерфейсов, таких как ChatGPT, выходя за рамки простого использования. Цель курса — научиться "строить завод" для создания сложных агентных систем, а не просто "водить готовый автомобиль" (использовать чат-ботов). Для этого разбирается клиент-серверная архитектура, процесс отправки запросов (API-вызовов) от клиента на сервер и получение ответов. Особое внимание уделяется формату данных JSON для структурирования информации, а также практическим навыкам отправки запросов напрямую к языковой модели с помощью команды curl в терминале, минуя веб-интерфейс.
Лектор подробно объясняет внутреннюю работу моделей, в частности, архитектуры «трансформер». Детализируется, как текст преобразуется в числовые векторы (векторизация) и как механизм «внимания» (attention) уточняет смысл слов в зависимости от контекста для предсказания следующего слова (токена). Обсуждается, как размер модели, объем и язык обучающих данных влияют на её точность, и почему возникают «галлюцинации» (ошибки). В конце подчеркивается важность понимания этих процессов и управления параметрами, такими как «температура», для эффективного создания собственных программ, взаимодействующих с LLM.
🔖 Ключевые тезисы
1. Архитектура и принцип работы чат-интерфейсов
  Клиент-серверная архитектура: Весь интернет и чат-интерфейсы построены на взаимодействии клиента (браузер на устройстве пользователя) и сервера (мощный компьютер в облаке).
  Процесс взаимодействия: Пользователь вводит текст в веб-интерфейс, клиент отправляет запрос ("телеграмму") на сервер, где работает языковая модель. Сервер обрабатывает запрос и отправляет ответ обратно, который клиент отображает пользователю.
  Анализ взаимодействия: Нажав F12 в браузере, можно открыть консоль разработчика и увидеть сетевые запросы ("телеграммы"), которыми обмениваются клиент и сервер, включая адрес, метод (POST) и содержимое.
  Токены: Ответ от сервера приходит по частям — токенами, которые являются частями слов. Модель оперирует именно токенами.
  Концепция курса "Строительство завода": Простое использование чат-ботов — это "вождение автомобиля". Цель курса — научиться "строить конвейер" (агентную систему), для чего нужно понимать, как работают "станки" (языковые модели) и как управлять ими напрямую.
2. Прямое взаимодействие с API
  Преимущества прямого доступа: Можно создавать собственные программы, гибко настраивать запросы, задавать свои системные сообщения ("промпты") и получать структурированные ответы, минуя универсальные веб-интерфейсы.
  Инструмент curl: Команда curl в терминале (командной строке) позволяет отправлять сетевые запросы напрямую на сервер языковой модели, минуя браузер.
  Структура curl-запроса: Включает адрес сервера (API endpoint), ключ авторизации (для идентификации и оплаты) и данные запроса в формате JSON.
  Посредники (дистрибьюторы) API: Сервисы вроде open-router предоставляют единый доступ к множеству языковых моделей от разных производителей, упрощая интеграцию.
3. Формат данных JSON
  Определение: JSON (JavaScript Object Notation) — это текстовый формат для структурирования и передачи данных между клиентом и сервером, легко читаемый как для человека, так и для машины.
  Структура: Основан на парах «ключ: значение». Значениями могут быть строки, числа, логические значения (true/false), массивы (списки в []) и объекты (наборы пар в {}).
  Применение в LLM: JSON позволяет точно задавать параметры запроса (модель, сообщения) и получать от модели структурированный, машиночитаемый ответ (например, {"sentiment": "positive"}), что гарантирует предсказуемость и упрощает программную обработку.
4. Внутреннее устройство языковых моделей (архитектура «Трансформер»)
  Токенизация и векторизация (Embedding): Текст разбивается на токены, каждый из которых преобразуется в числовой вектор — точку в многомерном пространстве. Это позволяет машине работать с текстом математически.
  Механизм «внимания» (Attention): Ключевая особенность трансформеров, предложенная Google в 2017 году. Модель анализирует весь текст целиком (параллельно), создавая таблицу связей между всеми словами для уточнения их контекстуального значения. Например, значение слова "кисть" уточняется в зависимости от слов "рука" или "маляр". Векторы слов корректируются на основе этих связей.
  Принцип предсказания следующего токена: Основная задача модели — предсказать вектор следующего токена. На выходе она генерирует распределение вероятностей для всех возможных следующих слов. Этот процесс многократно повторяется, каждый раз уточняя предсказание.
  Роль параметров (весов): Поведение модели определяется миллиардами параметров, которые настраиваются в процессе обучения на огромных массивах текста. Во время использования эти параметры не меняются.
  Контроль креативности («температура»): Модель может либо всегда выбирать самое вероятное следующее слово (сухой, точный текст), либо выбирать случайным образом из наиболее вероятных (креативный текст). Параметр «температура» позволяет управлять этим балансом.
  Современное состояние исследований LLM: Текущий этап сравнивается с ранними экспериментами с электричеством — многое работает, но фундаментальные причины успеха не всегда до конца ясны.
5. Причины ошибок ("галлюцинаций")
  Потеря связей: В длинном контексте модель может терять или некорректно обрабатывать связи между словами из-за огромного количества вычислений (сложность растет квадратично).
  Ограничения данных и обучения: Если модель обучалась преимущественно на данных одного языка (например, английского), связи для другого языка могут быть слабыми или отсутствовать.
  Пример с простой моделью: Локальная модель с 1 млрд параметров на вопрос «столица Эстонии» на русском ответила неверно, но на тот же вопрос на английском дала правильный ответ «Таллинн». Это показывает, что знание в модели есть, но оно не связано с русским языком из-за особенностей обучения.
6. Дополнительные концепции
  Различие между растровой и векторной графикой: Лектор провел аналогию для объяснения векторов. Растровая графика (JPEG) состоит из пикселей и теряет качество при увеличении. Векторная графика (PDF) основана на математических формулах и масштабируется без потерь. Векторы в LLM — это числовые представления, а не графические объекты.
  История разработки LLM: Идея трансформера родилась в Google, но OpenAI активно инвестировала в технологию и добилась прорыва, продемонстрировав потенциал масштабного обучения таких моделей.
  Эдвард Грант Сандерсон (3Blue1Brown): Упоминается как автор качественных визуализаций математических концепций, включая видео о работе языковых моделей.
❓ Вопросы
[Вставить Вопрос/Неясность]
📚 Задания
 1. Получить ключи доступа к API языковой модели и подключиться к Wi-Fi сети (пароль: VV060623).
 2. Открыть терминал (CMD в Windows, Terminal в macOS) и выполнить curl запрос к API языковой модели, используя пример из Telegram-чата.
 3. Научиться интерпретировать структуру JSON-ответа от модели.
 4. Осознать разницу между простым генеративным использованием ИИ (как в чат-ботах) и созданием агентных систем через прямое взаимодействие с API.
 5. Проанализировать, как возникают "галлюцинации" в языковых моделях, основываясь на объяснении механизма внимания и особенностей обучения.
 6. Посмотреть видеоролик Эдварда Гранта Сандерсона (3Blue1Brown) о том, как работает языковая модель, для лучшего понимания темы.
