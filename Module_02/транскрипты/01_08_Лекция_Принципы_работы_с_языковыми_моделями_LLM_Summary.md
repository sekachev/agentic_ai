01-08 Лекция: Принципы работы с языковыми моделями (LLM)
Дата и время: 2026-01-08 10:46:53
Местоположение: [Вставить местоположение]
Преподаватель/Инструктор: [Вставить имя]
📝 Сводка
В серии лекций обсуждаются принципы работы, управления и эффективного взаимодействия с языковыми моделями (LLM). Рассматриваются как фундаментальные механизмы, так и продвинутые концепции. Основное внимание уделяется параметрам управления генерацией текста (температура, top_k, top_p) для контроля креативности и вариативности ответов. Подробно разбираются внутренние ограничения моделей, такие как «галлюцинации», и методы их преодоления, включая концепцию «размышления» (reasoning) и важность системных промптов.
Далее проводится различие между реактивными генеративными системами, создающими контент, и проактивными агентными системами, способными воспринимать, размышлять и действовать в реальном или виртуальном мире для достижения целей. Демонстрируется потенциал агентных систем и концепции "Tool Calling" для автоматизации сложных задач, от управления браузером до комплексной разработки проектов в средах вроде AntiGravity. Также затрагиваются экономические и технические аспекты: классификация моделей на открытые и закрытые, методы их сжатия (квантизация) для запуска на доступном оборудовании, и структура стоимости использования LLM через API, включая цены на входящие, исходящие и кэшированные токены.
🔖 Ключевые тезисы
1. Управление генерацией текста и повышение качества ответов
  Проблема детерминированной генерации: Выбор всегда самого вероятного слова приводит к скучным, однообразным и предсказуемым текстам. Для "очеловечивания" ответов вводится элемент случайности.
  Механизмы управления случайностью:
      Температура (Temperature):   Основной параметр для контроля креативности. Низкая температура повышает вероятность выбора менее стандартных слов, делая модель "креативнее", а высокая — усиливает разницу между вероятностями, делая модель более предсказуемой.
  Top-K семплирование: Ограничивает выборку для генерации K самыми вероятными словами.
  Top-P (Nucleus) семплирование: Ограничивает выборку словами, чья суммарная вероятность достигает порога P.
  Ограничения и "галлюцинации": Модели обязаны генерировать ответ, и при нехватке знаний могут придумывать факты. Увеличение размера моделей для решения этой проблемы непрактично из-за экспоненциального роста сложности и ресурсов.
  Метод «Размышления» (Reasoning): Аналогично человеческому черновику, модели предоставляется возможность внутреннего анализа задачи, рассмотрения разных путей решения и отбрасывания неверных вариантов перед формированием чистового ответа. Это позволяет повысить точность без показа промежуточных ошибок пользователю.
  Важность системных промптов: Системные инструкции задают модели "роль" (например, ассистента) и контекст, что кардинально влияет на качество и релевантность ответов. Создание кастомного системного промпта для узкой задачи часто эффективнее стандартных.
2. Промпт-инжиниринг и стратегия взаимодействия
  Классификация подходов:
      Zero-shot:   Промпт без примеров. Низкая вероятность качественного ответа.
  One-shot: Промпт с одним примером. Значительно повышает качество.
  Few-shot: Промпт с несколькими примерами. Вероятность успеха еще выше.
  Использование "персоны": В современном промпт-инжиниринге указание "персоны" (например, "ты эксперт...") считается скорее вредным, так как сужает возможности модели, особенно при наличии примеров.
  Стратегия "знать, чего не знаешь": Перед тем как просить у ИИ готовое решение, рекомендуется сначала исследовать предметную область. Качество ответа напрямую зависит от уровня осведомленности пользователя и точности запроса.
  Динамический контекст: Эффективнее предоставлять модели конкретную инструкцию для конкретной задачи в нужный момент, а не перегружать ее одной огромной статичной инструкцией.
  Контекстное окно: Вся история переписки прикрепляется к каждому новому запросу. С увеличением этого "бутерброда" сообщений качество работы модели падает. Для сохранения качества большие задачи необходимо разбивать на мелкие (BRD -> Roadmap -> TUDU).
3. Генеративные и Агентные системы
  Генеративные системы: Реактивны. Ждут промпт от пользователя и генерируют контент (текст, изображения) на его основе.
  Агентные системы: Проактивны. Действуют для достижения цели, а не в ответ на стимул. Они работают по циклу Perceive (Восприятие) -> Reason (Размышление) -> Act (Действие), иногда с шагом Learn (Обучение). Результат их работы — действие в реальном мире, а не просто контент.
  Концепция "Tool Calling" (вызов инструментов): Механизм, позволяющий модели использовать внешние функции (например, узнать погоду, управлять браузером, запускать код). Модель генерирует команду, приложение ее выполняет, а результат возвращается модели для формирования ответа.
  Потенциал агентов: Комбинация мультимодальности (способность "видеть" экран) и инструментов управления (мышь, клавиатура) позволяет создавать агентов, способных автоматизировать рутинные задачи, выполняемые человеком за компьютером (например, работа в бухгалтерских программах).
4. Технические и экономические аспекты
  Открытые vs. Закрытые модели:
      Закрытые:   Доступны только через API за плату (e.g., ChatGPT).
  Открытые: Можно скачать и запустить локально. Разрыв в качестве между ними и закрытыми моделями сокращается (около 6 месяцев задержки).
  Сжатие моделей (Квантизация): Процесс округления весов модели для уменьшения ее размера и требований к оборудованию. Экстремальное сжатие (до 1 бита) позволяет запускать LLM даже на смартфонах, хотя и с потерей производительности.
  Стоимость использования API:
      Input price:   Цена за токены, поданные на вход (промпт, контекст).
  Output price: Цена за сгенерированные токены (ответ), обычно выше.
  Cache rate price: Значительно более низкая цена за повторную обработку контекста в рамках одной сессии.
  Мультимодальность и встроенные функции: Современные модели (например, Gemini) могут принимать на вход текст, изображения, видео и аудио, а также имеют встроенные "под капотом" функции, такие как поиск в Google, выполнение кода (Code Execution) для вычислений, что упрощает их использование.
  Формат Markdown: Простой текстовый формат разметки, используемый моделями для генерации структурированного и форматированного текста (заголовки, списки, код) с низким потреблением токенов.
❓ Вопросы
[Вставить вопрос/неясность]
📚 Задания
 Поэкспериментировать с параметром температуры, чтобы увидеть его влияние на креативность ответов модели.
 Проанализировать работу системных промптов и попробовать создать свой для специфической задачи.
 Изучить и попрактиковаться в написании промптов с примерами (one-shot, few-shot).
 Перед решением сложной задачи с помощью ИИ, сначала использовать его для исследования предметной области ("Что мне нужно знать, чтобы...").
 Изучить концепцию "Tool Calling" и попробовать использовать инструменты в среде разработки, например, в AntiGravity.
 Ознакомиться с агентными браузерами (например, от ChatGPT или Arc).
 Попробовать реализовать автоматизацию проекта: создать документацию (BRD), дорожную карту (Roadmap) и список задач (TUDU) с помощью агента.
 Поиграть с маленькими открытыми моделями (например, на Hugging Face), чтобы оценить их возможности и ограничения.
 Изучить документацию по промптингу и Function Calling, а также материалы на OpenRouter для понимания выбора моделей и провайдеров.
 Изучить и попрактиковаться в использовании синтаксиса Markdown для форматирования текста.
