Вот готовый учебный материал в формате Markdown. Он написан так, чтобы его можно было использовать для любого потока студентов. Стиль изложения — от теории к практике, объясняя "магию" Linux простым языком.

```markdown
# 🚀 Превращаем код в реальный сервер: API и Systemd

## 🎯 Введение: Зачем нам это нужно?

Вы уже написали свой первый код на **FastAPI**. Но что это вообще такое?

**API (Application Programming Interface)** — это "окошко" во внешний мир для вашей программы.
*   **Извлечение информации:** Через API фронтенд (сайт) или мобильное приложение запрашивает данные из базы.
*   **Хранение информации:** Через API пользователи отправляют данные (регистрация, лайки, заказы) на сервер.

Сейчас ваш скрипт работает, только пока открыто черное окно терминала. Стоит вам закрыть браузер или нажать `Ctrl+C` — и ваш API "умрет". Сайт станет недоступен.

**Наша цель:** Сделать так, чтобы программа работала **вечно**, запускалась сама после перезагрузки сервера и восстанавливалась, если вдруг упадет. Для этого мы превратим обычный Python-скрипт в **системную службу (Service)**.

---

## 🛠 Шаг 1. Текущая ситуация (Ручной запуск)

Сейчас вы запускаете сервер командой:
```bash
python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
```
Вы видите логи в реальном времени. Это отлично для разработки, но плохо для "продакшена" (реальной работы).

Мы будем использовать **Systemd** — это главный менеджер процессов в Linux. Он умеет следить за программами в фоне.

У вас в папке `/root` уже подготовлен файл-шаблон `fastapi.service`. Давайте разберем, что у него внутри.

---

## 📄 Шаг 2. Анатомия файла службы

Файл `.service` — это инструкция для Linux. Откройте его командой `cat fastapi.service` и посмотрите.

Вот что значат эти строчки (читаем внимательно!):

### Секция [Unit] — Описание
Здесь мы рассказываем системе, *что* это за служба.
*   **`Description=FastAPI Student Server`** — Имя службы, которое будет видно в логах.
*   **`After=network.target`** — Важная настройка! Она говорит: "Не запускай этот сайт, пока на сервере не появится интернет". Без этого сервер может попытаться запуститься раньше времени и упасть с ошибкой.

### Секция [Service] — Самое мясо
Здесь описано, *как* запускать программу.
*   **`User=root`** — От чьего имени запускать. В учебных целях мы используем `root` (администратор), но в больших компаниях здесь создают отдельного пользователя для безопасности.
*   **`WorkingDirectory=/root`** — Папка, где лежит ваш файл `main.py`. Systemd сначала перейдет в эту папку, а потом выполнит команду.
*   **`ExecStart=/usr/bin/python3 ...`** — Сама команда запуска. Обратите внимание: мы указываем **полный путь** к Python (`/usr/bin/python3`), чтобы система точно знала, какой интерпретатор использовать.
*   **`Restart=always`** — "Режим зомби". Если ваша программа вылетит с ошибкой, Linux тут же поднимет её снова.
*   **`RestartSec=5`** — Пауза 5 секунд перед воскрешением (чтобы не перегрузить процессор вечными перезагрузками).

### Секция [Install] — Автозагрузка
*   **`WantedBy=multi-user.target`** — Означает, что служба должна стартовать в обычном многопользовательском режиме (когда сервер полностью загрузился и готов к работе).

---

## ⚙️ Шаг 3. Установка и запуск (Практика)

Выполните эти команды по очереди в вашем терминале.

### 1. Копируем файл в систему
Linux хранит настройки служб в папке `/etc/systemd/system/`.
```bash
cp fastapi.service /etc/systemd/system/
```

### 2. Перечитываем настройки
Сообщаем Systemd, что появился новый файл.
```bash
systemctl daemon-reload
```

### 3. Включаем и запускаем
Команда `enable` ставит службу в автозагрузку (чтобы работала после перезагрузки VPS).
Команда `--now` запускает её прямо сейчас.
```bash
systemctl enable --now fastapi
```

---

## ✅ Шаг 4. Проверка и отладка

### Как узнать, что всё работает?
Спросите статус у системы:
```bash
systemctl status fastapi
```
🟢 Если видите зеленый кружок и надпись **active (running)** — поздравляем! Вы стали системным администратором своего API.

🔴 Если видите **failed** или **dead** — где-то ошибка.

### Как читать логи?
Так как программа работает в фоне, она больше не пишет в терминал. Чтобы увидеть, что происходит (или почему она упала), используйте команду:
```bash
journalctl -u fastapi -f
```
*   `-u fastapi` — показать логи конкретно нашего юнита.
*   `-f` — (follow) следить в реальном времени (как в матрице). Нажмите `Ctrl+C`, чтобы выйти.

---

## 🌐 Шаг 5. Финал

Теперь ваш сервер работает автономно. Вы можете закрыть вкладку терминала, перезагрузить компьютер — API все равно будет доступен.

Проверьте результат в браузере по вашему адресу:
👉 `https://api.ваше_имя.qix.ee`

Там должно быть сообщение: `{"message": "It works!" ...}`.
```
